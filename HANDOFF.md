# HANDOFF — 识别链路现状与交接

> **更新：2026-08-22 · 分支 `main`，最新 commit `7e6a2cf`。本节是当前交接的
> 唯一入口。** 下方 2026-08-21 及更早的章节是历史记录，**不代表当前状态**，
> 尤其 §16 的结论已被本轮推翻（见下）。先读本节，需要细节再翻 design 文档。

## 这一轮（2026-08-22）做完的事

14 个提交，`c7d4547`..`7e6a2cf`。全量后端 **1117 passed / 0 failed**，
前端 vitest **88 passed**，vue-tsc 通过。

### design/31 预览比价（先比价、后逐行确认）

`preview_sandbox` / `preview_service` / `preview_ordering` 三个新模块。核心是
**在沙箱里跑官方那条链路本身**（confirm → 对齐 → 矩阵），跑完整体回滚，
不另写一份"只读近似比价"。`basis="preview"` 在契约层禁止 firm 推荐。

**两个坑留在文档里**（design/31 §7、`preview_sandbox.py` 模块文档）：
pysqlite 隐式 BEGIN 会让"外层事务 + SAVEPOINT"名存实亡（第一版沙箱是漏的，
行数比对测试抓到的）；沙箱里一旦出现秒级慢 I/O（embedding 走 HTTP），
并发写入会**失败**而不只是变慢。

### design/32 没有采购清单也能对齐

实测：同项目各家报价的行数与数量序列跨供应商 100% 一致（Excel↔Excel）。
据此实现 A1（`qty is None` 不是条目行，两侧共用词表 `ingestion/list_rows.py`）
+ A2（报价派生轴，复用 `_sequential_matches`，零新增对齐算法）。
CLAUDE.md §4 Identity 已按用户决策修订：一个比价一个行轴，结果必须声明
`axis_kind`；`quote_derived` **只能进预览**，契约层拦。

### 一条根因，解释了这轮全部四次超时

**design/29 §16「GIL / pdfium 锁竞争」的结论已撤回。** 真因是
`upload_document` / `classify_tier0_upload` / `summarize_facts` /
`tender_list_match` 把同步阻塞重活写在了 `async def` 里——FastAPI 的
`def` 路由进线程池、`async def` 直接跑在事件循环上，所以卡住的不是一个请求，
是**整个服务器**（连 `/api/health` 都不响应）。一次拖 4 个文件 = 4 次视觉
调用 × 约 7 秒，近半分钟全停。`test_routes_not_async.py` AST 扫描守着回归。

### 工作台

三段式（上传 / 预览 / 对比，**不强制线性**，step 可随时点回去）；采购清单
识别完**自动**设为比价基准（用户明确"不需要手工确认"，但卡片上常驻
"✓ 已作为比价基准（N 项）"，自动可以无声不行）；待确认项可展开**原文依据**
（页/行 + 该行识别到的全部字段，未读到的红字标出）；预览页不显示历史列。

## 本轮我判断错过的地方（照实记，别当前提）

1. **「校对入库在工作台里没有入口」——错。** `WorkspaceView.vue:1051` 就是
   那个按钮，在明细视图里。我只看了解构导入没往下翻模板。
2. **「超时是 GIL/锁竞争」——错。** 见上，真因是事件循环被独占。锁竞争只会
   让请求变慢，不会让请求**得不到处理**；而现象是所有请求同时停。
3. **「对齐规则有问题」——错。** 泰科龙被顺序直连拒绝是**正确**的：那 9 行
   是连续块状错位（#44-49 闸阀↔球阀、#82-84 止回阀↔自动排气阀），不是命名
   差异。规则挡对了。
4. **「改用 Excel 报价清单」——绕开了题目。** 比价扫描件正是这个系统要解决
   的问题，换输入源等于换题。已撤回。

## 未解决：泰科龙扫描件为什么只有 76.4%

这是当前**最具体、最值得接手的一条线索**。

| 指标 | 数值 | 说明 |
|---|---|---|
| 行召回 | 泰科龙 100%(89) / 凯硕 100% / 绵存 97.8%(87) | **跟历史基线一致，识别没退化** |
| 锚点(招标PDF) vs Excel 标准答案 | **100%** | 招标侧识别是完美的 |
| 各家 **Excel** 报价 vs 锚点 | **100% / 100% / 100%** | 对齐链路本身没问题 |
| 泰科龙 **投标PDF** vs 锚点 | **76.4%** | 问题在这里 |

**已排除**：页面旋转（`/Rotate=270` 全页一致，渲染出来 2382×1684 横向，
方向正确）；页序错乱（source_ref 页序单调 5→6→7→9→10→12→13→14）。

**已定位**：**第 8 页和第 11 页零产出**。

```
第5页 10条  第6页 13条  第7页 26条  [第8页 缺失]
第9页  9条  第10页 16条 [第11页 缺失]
第12页 3条  第13页 7条  第14页 5条   合计 89
```

合计恰好 89 条（有页丢行、别处多行，净额相抵），所以**行数看起来是对的，
但第 i 行的内容不是第 i 项**。块状错位的位置正好紧挨缺页：#44-49 在第 7 页
row21-26（第 8 页之前），#82-84 在第 13 页（第 11 页之后）。

**下一步要分开的两种可能**（查一次即可）：

1. 页面角色分类把第 8/11 页判成了非清单页（封面/资质/骑缝章），根本没送去抽表；
2. 送去抽了，但表格识别在这两页返回空。

第 1 种是分类阈值问题，第 2 种才是识别质量。**这跟"提准确率"不是一回事——
不是把模型调更准，是有两页被跳过了。**

## 已知问题，未处理

1. **识别准确率相关缺陷已记 `TODO.md §-1`，用户明确暂不排期**：凯硕 4 行
   含税合价丢失（差 25,540 元 / 2.74%，声明总价门会继续报警，**不要为了让它
   变绿放宽阈值或补值**）；#89 列串位导致 qty 丢失，该行照常入库但**进不了
   报价派生轴**（派生轴要用数量校验对齐），于是"入库 89 条、矩阵 88 行"，
   3,460 元在矩阵里看不见且界面无说明。
2. **对比段的 AI 总结未接线**（`/analysis/bid-insight` 接口已有）。那一段现在
   放的是一条明说此事的 alert，不是空面板。
3. **design/31 §7 的 SQLite 写锁**：实测持锁 ~50ms 安全，但前提是链路里没有
   慢 I/O。embedding 在沙箱事务内是真实风险，两个解法（挪出事务 / 调
   busy_timeout）都没实施。
4. **`npm --prefix apps/www test` 在 CLAUDE.md §3 里写着，但 package.json 没有
   `test` 脚本**，实际要用 `npx --prefix apps/www vitest run --root apps/www`。
5. **工作区有一批开工前就存在的未提交改动**（`tests/fixtures/documents/` 语料
   扁平化、`docs/*.docx`、`docker-compose.yml`、`scripts/deploy.sh`、
   `apps/api/intelligence/` 下 7 个文件等），本轮没碰也没提交——不清楚是否到了
   可提交状态。

## 环境提醒

- 固定端口：后端 8000 / 前端 3000，**不开 `--reload`**（CLAUDE.md §3）。
- `get_settings()` 是进程级懒单例，改 `.env` 必须重启后端。
- 后端加了新路由必须重启才生效；纯前端改动 Vite 热更新即可。


> **更新：2026-08-21 · 分支 `codex/recognition-engine-eval`（已合并入
> `main`，最新 commit `312b193`）。本节是当前交接的唯一入口，2026-08-11
> 及更早的内容（下方全部章节）是识别引擎选型那一轮的历史记录/方法论教训
> ——那个问题**已经解决**（见下），不代表还是开放问题，不要把旧章节的
> 数字/结论当成当前状态。**先看本节，需要细节再翻对应 design 文档，不要
> 从下方老章节反推现状。**
>
> **fixture 目录已知问题已由用户自行解决**：`tests/fixtures/documents/
> {tender,bid,bid_list,tender_list}/` 四个子目录不再存在——用户把 14 份
> 语料改成扁平命名（`{项目名}-{文档类型}.ext`）直接放在 `documents/` 下，
> 测试脚本路径引用已同步（commit `c69f53b`）。下方"已知问题，未处理"里
> 那一条已过时，不代表当前状态。

---

> 以下为 **2026-08-21 及更早**的历史记录，保留作为方法论与实验数据。
> **不代表当前状态**——尤其 design/29 §16 的「GIL/锁竞争」结论已在
> 2026-08-22 撤回（真因是阻塞调用写在 async 路由里，见本文件开头）。

## 这一轮（2026-08-14 ~ 2026-08-21）做完的事，按时间顺序

1. **design/26（识别引擎从 qwen 换 Paddle）全量落地并接生产**——本文件
   2026-08-11 那一版还在纠结"方向判定不稳定""换引擎值不值"，现在已经
   是 `pipeline.py::extract_quote` 直连 `paddle_ocr.submit_and_parse`，
   不经过 `LLMProvider` 抽象；招标侧同样切了 Paddle。qwen 没有整体删除，
   降级为兜底/辅助角色（页面角色分类、方向预检、design/29 新加的 Tier
   1.5 扫描件粗判定都还在用 `DashScopeOCRProvider`）。**详细现状**：
   `.claude/rules/recognition.md`（路径本身有权威说明，比这里任何转述
   都准）。
2. **design/27（工作台重设计，供应商轴替代步骤轴）全部交付，步骤5
   （退役旧5步向导）已完成**——`views/compare/IndexView.vue` 已物理
   删除，`/compare` 系旧路径改纯跳转到 `/workspace`，侧边栏只剩一个
   "招标比价分析"入口。Univer 选型定了（`quoteGridController.ts` 是
   唯一碰 Univer API 的模块，其余代码只认它导出的接口，方便以后换
   AG Grid）。
3. **design/28（拖拽文件自动分类三级判据）全部 7 刀交付**——Tier 0
   （Excel 填充率零模型判据）+ Tier 1（识别后信号融合）+ 供应商归属 +
   Excel-primary 校核 + `POST /api/intake/classify-tier0` + 三场景 E2E。
   Tier 2（LLM 兜底）**测量后决策不做**——真实语料残留率 0，见
   `docs/design/28` §9。
4. **design/29（统一上传入口 + 卡片概述 + 统计）全部 5 刀交付，中途有
   一次真实测量推翻了原计划**：
   - cut 1 本来要做"扫描件 PDF 自动招标/投标分类"，**真实语料 7 份投标
     扫描 PDF 实测 0/7**——不是噪声，是系统性误读（机电投标封面惯例上
     会重印招标方的双字段封面模板并填供应商名，模型认成"这是招标文件
     自带的投标封面样式"）。原计划因此暂停自动派发。**⚠ 这条结论
     2026-08-21 已被推翻，见下方第 6 条——不是"能力不够"，是"送的
     信息不够"。**
   - 用户随后明确要求"给出弹出问询"——**加了个二选一弹窗**替代方案：
     原生 PDF 走真实判据自动路由，扫描件/判不出的一律弹"招标文件/投标
     文件"两选一，不再假装能自动分类，也不再让用户自己回去找卡片。
   - 卡片概述用**已确认的结构化字段模板化拼给纯文本 LLM**组织成一两句
     话，不是让模型重新读原文——避免幻觉、避免成本翻倍。
   - **2026-08-21 真实手测又推翻了一版实现**：默认页面显示 4 家供应商
     的完整 QuoteGrid 明细表格（每个都是一整个 Univer 引擎实例）导致
     严重卡顿——AntD `a-tabs` 默认全量挂载所有 pane，不是只挂当前
     激活的。改成"明细+确认入库是点卡片之后的下一步"，且下一步里也
     只挂载当前激活 tab 的那一个 QuoteGrid（同一时刻最多 1 个实例）。
     详见 `docs/design/29` 状态横幅，commit `8cecc7d`。
5. **生产部署——bid-compare 迁移到跟 pixel-lora 共宿主的 ECS**
   （`106.14.113.209`，域名 `bid.hotcrp.cn`，HTTPS 证书已装）。详细
   架构、风险点、已知脆弱点全部记在 `docs/DEPLOY.md`，这里只写**当前
   最新状态**，因为这个文件更新频率跟不上部署本身：
   - GitHub Actions（`.github/workflows/deploy.yml`）**已配置但从未
     真正跑通过**——GitHub Secrets（`ACR_REGISTRY`/`ACR_USERNAME`/
     `ACR_PASSWORD`/`ECS_HOST`/`ECS_SSH_KEY`）**还没配置**，我不会替
     用户填真实密钥值（见下方"红线"）。
   - **第一次真实部署是手动做的**：直接 SSH 上 ECS，在 ECS 本机
     `docker build`+`docker push`（用 ECS 上已经登录过的 ACR 会话，
     没有经手任何新密钥）+ `docker compose up`。过程中现场发现并修了
     3 个真 bug：`uv.lock` 落后 pyproject.toml 缺 `pyjwt`/
     `pdfminer-six`/`pdfplumber`（commit `a8ec609`）、`data/` 目录属主
     是 root 容器非 root 用户写不了 SQLite、ACR `bidcom` 命名空间没开
     自动创建仓库（用户在控制台手动开的）。
   - **⚠️ 关键：git `main` 已经比 ECS 上实际跑的容器新好几个 commit**。
     ECS 上 `/opt/mempas` 的 git 现在停在 `381f25b`（Dockerfile 合并那次），
     **design/29 的全部工作（cut1/cut3-5/今天的4条UI修复，即 `c3bdc9a`/
     `b054218`/`8cecc7d`）都还没有构建、没有部署**。`bid.hotcrp.cn`
     现在看到的还是design/29 之前的旧界面。要看到最新效果需要重新走一遍
     手动部署流程（或者先把 GitHub Secrets 配上，让 CI 真正跑起来）。
6. **扫描件 PDF 招投标分类 0/7 → 8/8，第 4 条的"暂停自动派发"结论撤回**
   ——用户直接质疑"是不是没有利用好 LLM"，逼着重新测量而不是接受初版
   结论。根因不是模型能力：初版只送第 1 页 2MP 缩略图、单发一次；改成
   送前 `SCANNED_CLASSIFY_PAGES=3` 页原生分辨率（6MP 预算）+ 点破"投标方
   常重印招标方封面模板"这个具体误判模式的提示词后，同一批语料、**同样
   用 flash 档模型**（用户明确要求先试 flash，不是换模型），复测
   **8/8**。两级验证：独立复现脚本 + 生产真实调用路径
   `classify_pdf_for_dispatch`，结果一致。`routes/intake.py::
   classify_tier0_upload` 已接回真实分类调用（此前是 `call=None` 强制走
   兜底弹窗）；`test_scanned_pdf_classify.py::TestScannedPdfRealAccuracy`
   落了 8 条可重放回归（`@pytest.mark.e2e`，需要真实 `DASHSCOPE_API_KEY`，
   默认跳过）。commit `6a4fb53`。
   - **踩坑**：修完代码后前端已通过 Vite HMR 热更新，但用户手测反馈"还是
     没识别"——根因是本地 dev 后端进程在 `.env` 填真实 `DASHSCOPE_API_KEY`
     之前就已启动，`get_settings()` 是进程级懒单例，不会自动重读；`uvicorn`
     本项目约定不开 `--reload`。重启后端后确认生效（`/api/health` 显示
     `llm_provider: dashscope_ocr`，真实上传返回 `"verdict": "bid"`）。
   - 手测又发现两个更细的 bug：① 用户把 14 份语料改成扁平命名
     （`{项目名}-{文档类型}.ext`，不再分子目录）后，`useSupplierUpload.ts`
     的 `_extractSupplierHintFromFilename` 假设"供应商名在前"，新命名下
     会把项目名当供应商名，导致所有投标卡片识别中阶段显示同一段文字——
     改成先按字面 `-` 切出项目名后再复用既有提取逻辑，无 `-` 的旧命名
     向后兼容。② 识别中卡片只显示裸百分比数字，看不出是"分析中"还是
     真的分类失败——加了 `uploadedFileCount` 文件计数提示 + 真实
     `a-progress` 进度条 + `stage`/`stageDetail` 文案。两条 commit
     `c69f53b`（测试脚本路径同步扁平命名，未跑测试验证，按用户要求）/
     `312b193`（前端两处修复，type-check + vitest 66/66 全绿）。

## 红线：密钥/凭证，我不会替用户碰

这轮部署过程中反复出现"能不能直接把密钥用起来"的场景（ACR 凭证、
DASHSCOPE/BAIDU API key、把 `.env` 提交进 git 跟 pix 学）——**全部
拒绝了，不是这个项目单独定的规矩，是通用红线**：不解码、不复制、不
写入任何文件或 GitHub Secrets、不建议把密钥提交进版本库（即使对方举例
说"另一个项目就是这么做的"）。`apps/api/.env` 在 ECS 上现在还是占位符
（`DASHSCOPE_API_KEY=sk-replace-me` 这种），OCR 识别功能理论上跑不通，
需要真人 SSH 上去填。下一个接手的人如果也被要求做类似的事，按同一条
线处理：告诉用户具体要填哪、在哪填，不替他们填。

## 已知问题，未处理（如实记录，不是遗漏）

1. ~~fixture 扁平化后测试未验证~~ ——**已跑、发现漏网、已修（2026-08-21）**：
   `pytest apps/api/tests tests -q` 实测 `2 failed / 1009 passed`，两条都是
   `test_paddle_quote_api_e2e.py` 的 `StopIteration`——`PDF_DIR` 仍指向已空的
   `tests/fixtures/documents/bid/`，`c69f53b` 同步别处引用时漏了这一处。改指
   扁平后的 `documents/` 后两条通过（`2 passed`），四个空子目录已删。这条印证
   了当时那句"改完引用不等于验证过"。
2. ~~design/29 扫描件 PDF 分类管线代码保留但未启用，模型准确率不够
   （0/7）~~ ——**已撤回，见上方第 6 条**：根因是送的信息不够（1 页缩略图、
   单发），不是模型不行；送 3 页原生分辨率 + 点破失败模式的提示词后复测
   8/8，已接回生产真实调用路径（`routes/intake.py::classify_tier0_upload`），
   工作台现在会先尝试真实分类，判不出来才弹二选一兜底弹窗。
3. **design/29 的"点卡片进详情"这条链路没有真实浏览器 E2E 验证**——这个
   session 的浏览器自动化工具确认没有可靠的文件系统文件注入能力（找到
   4 个 `<input type=file>` 但没有对应的上传接口），拖拽上传测不了。
   后端逻辑、前端类型检查、vitest 都过，但完整"拖文件→分类→弹窗/自动
   路由→卡片渲染→点卡片进详情→只挂 1 个 QuoteGrid"这条链路本身没有
   浏览器级别的直接证据。**2026-08-21 追加**：确认可以用 `DataTransfer`
   注入合成文件到 `<input type=file>` 触发 `change` 事件（不是没有能力，
   是没有真实几 MB 的语料 + 实付费视觉模型调用不值得为一次可视化确认
   现做），空态下的文件计数行渲染逻辑已用这种方式验证过；完整上传→
   识别→卡片渲染全链路仍未做，如需要请用真实 fixture 走一遍，预期单份
   20-90 秒。
4. **GitHub Actions 部署链路从未真正跑通过一次**——见上方部署章节，
   Secrets 没配，只验证过手动 SSH 部署这一条路径管用。ECS 上
   `/opt/mempas` 的 git 仍停在 `381f25b`，本轮全部工作（design/29 全部
   + 0/7→8/8 修复 + 两处前端手测反馈修复，即 `c3bdc9a`..`312b193` 这一
   串）都还没有构建、没有部署，`bid.hotcrp.cn` 上看到的还是更旧的界面。

---

> 以下为 2026-08-11 及更早的历史记录（识别引擎选型那一轮），保留作为
> 方法论教训与详细实验数据，**不代表当前状态**——见上方新章节。

> 更新：2026-08-11（最佳实践评审批次1-6 全部完成）· 分支 `codex/agent-rebuild`
>
> **一句话**：VL-direct 是**报价与招标两侧唯一的识别路径**，走通了生产 HTTP
> API 全链路（上传→任务→确认→对齐→矩阵→导出）；legacy 报价/招标分支（含
> `table_recognizer.py`/`table_parser.py`/`page_classifier.py`/`adaptive_tiler.py`/
> `aggregator.py`/`prompts.py`/`intelligence/snapshot_provider.py`/`splitter.py`）
> **已物理删除**（最佳实践评审 F1：两个生产 provider 都实现 `vl_extract_csv`，
> legacy 分支在生产从未可达，"demote to cross-check" 的前提本身不成立，见
> docs/design/21 状态横幅）。**但方向判定仍不稳定**（同配置同文档跑出 3/10 与
> 10/10），且抽取本身有 0.18% 的运行间方差——两者都还没有解法，只有门在兜底。
>
> **不要把本文任何数字当作系统能力承诺。** 准确率数字来自单次或少数几次运行；
> 稳定性结论一律需要多次运行的分布，见 §0 的撤回表。
>
> **最佳实践评审 6 个批次已全部执行完毕**（commit 7d2adb0..17c8a68）：
> C1/D1 导出弱门与有损重建、F1/F2/F4 legacy 物理删除、C2/D2-D5 重复实现合并、
> G1 鉴权测试隔离、N2/N3/N7 命名与接口声明、services/ 按域包化。**E1-E4（错误
> 契约统一）与 B3（身份键改名）明确推迟**，未做，不是遗漏——两者体量堪比整个
> C/D 系列，需要专门一轮。详见各 commit message 与 `docs/design/22`。
>
> **方法论缺口（本轮末尾发现，记录不隐藏）**：`pyproject.toml` 的 `testpaths =
> ["apps/api/tests", "tests"]` 有两处，本轮此前所有 `pytest apps/api/tests -q`
> 命令因显式传路径覆盖了 config，**从未跑过根目录 `tests/`**（17 文件/151 用例）。
> 批次6 末尾才发现并两处一起跑通。今后校验一律用 `pytest apps/api/tests tests -q`
> 或不传路径依赖 config。

---

## 合并检查点（2026-08-12，`codex/agent-rebuild` → `main`）

> 长期挂账的"4 个既有失败"（本文其他章节仍会提到旧数字，如 §1 的 811、
> §5 的 35/613——那些是各自章节写下时的历史快照，不回填）在这次合并前审计
> （Fable 复核）中查明并清零：`test_profession_map`/`test_category_abbr`
> 断言 `len(PROFESSION_MAP)==10`，品类表合法增长到 11 个后没跟上；
> `test_quote_fact_derives_total_price`/`test_basic_csv_no_total_row` 断言
> `total_price` 会被静默派生，但 `quote_fact.py:129-134` 记录这是 2026-08-09
> 有意移除的行为（CLAUDE.md"禁止未经确认自动覆盖原值"）。两组都是测试断言
> 没跟上有意变更，不是回归——已改写断言，不是放宽或跳过。

**当前真实基线**：`pytest apps/api/tests tests -q` → **815 passed, 0 failed,
1 skipped, 7 deselected(@e2e)**，333.17s。`apps/www`：`vue-tsc -b` 干净，
`vitest run` 6 files / 65 tests 全绿。这是合并到 main 那一刻的基线，之后
任何一轮报告"既有失败"前，先确认是不是又需要一次这样的审计，不要不假思索
沿用本文其他章节的旧数字。

**识别引擎替换探索（design/25 前置调研，未落地代码）**：客户反馈现有 PDF
识别耗时慢，对照 WPS。排查发现：① 招标文件原生 PDF 走文字层直抽，7 月百度
实验验证 17.7s/100% 可行；② 候选模型 PaddleOCR-VL 在行级序号召回率上
7 份投标 PDF 里 6 份达标（含项目最难样本泰科龙转置表格 100%），耗时优势
9-20 倍；③ 拼图省时间的路线已否决（分辨率与召回绑定，大图上传本身会失败）；
④ 逐字段准确率解析器有已知列对齐缺陷，数字不可信，只有行级/序号级召回率
可信。证据链：`scripts/{try_paddleocr_vl,score_paddleocr_vl,stitch_vs_multi_bench}.py`
+ `outputs/{baidu_paddleocr_vl,baidu_unlimited_ocr}/`（gitignore，不随代码走，
需要复现请重跑脚本）。是否立项、Phase 划分见后续 `docs/design/25`（未写，
待讨论：轨A 是否独立先行、Phase 2 通过线数字、亨通 65.4% 召回率归因）。

---

## 现状速览（2026-08-11）

| 链路 | 识别器 | 最近一次实测 |
|---|---|---|
| 投标报价 | VL-direct（唯一路径） | 电缆四份 136/136；阀门三份 89/89 |
| 招标采购清单 | VL-direct（唯一路径） | 89/89，序号 1..89 全中，数量总量 1,644 一致 |
| 招标封面四标量 | VL-direct（唯一路径） | 招标与邀标共用同一份解析器 |
| 招标要求（品牌等） | VL-direct（唯一路径） | 声明为数据，加一项 = 加一条配置 |
| Excel 交叉校验 | 与识别器无关 | VL 的行同样对账 |

legacy 已删除，不再是"仅剩两处用途"——那两处用途（provider 不具备多图调用时的
逐页批量兜底、`services/tender/tender_pdf.py` 的内部路径，批次6 后已改到
`services/tender/` 域包）经核实全部不可达，删除前逐一验证过。`.claude/rules/
recognition.md` 仍保留 TableGrid/bbox 相关的规范性表述——批次4 只做了 N2/N3/N7，
没有改写这份规则文件，仍待专门一轮（与 E1-E4/B3 一起排期）。

**当前已知的两个不稳定源**（都不是代码缺陷，是模型行为）：
1. 方向判定：同输入多次运行结论不同，未解决，靠"无过半共识则不转并标 REVIEW"兜底。
2. 抽取方差：同图同提示词 temperature=0，亨通两次跑出的金额差 37,632 元（0.18%）。
   因此**准确率不能用冻结快照做红绿门**——见 `test_cable_accuracy_e2e.py` 的分层。

**近期设计文档**：
- `docs/design/20` checksum 阈值形状（草稿，埋点已上、阈值未改）
- `docs/design/21` legacy 退场规划与已知缺口
- `docs/design/22` 最佳实践评审范围

---

## 0. 先读这一节：已撤回的结论

**第四轮撤回**（2026-08-10，生产链接入 + 单变量 A/B）：

| 撤回内容 | 真相 |
|---|---|
| 「给模型一列 uncertainty 申报疑问，就能阻止它破坏 CSV 格式」 | **证伪。** 六次单变量 A/B（方向固定、每臂三次）：**无此列的对照臂同样没有格式崩溃**，两臂缺格行都是 0/0/0、金额差都是 0/−52/0。当时只有单臂数据，把相关当成了因果 |
| 「上海浦东 p6 有 20 行列错位是版式问题」 | **真因是方向。** 那次只转了 3 页（正确应转 13 页）。模型在读侧躺和倒置的页面，"出声思考、改用竖线分隔、边写边推翻自己"是**症状不是病因**。方向固定后同一份文档六次运行：最佳副本恒为 **136 行（=golden）**，四次金额 **±0.00**、两次 −52 元 |
| 「台账 137 行进 137 行出，0 丢行」可作为识别质量证据 | **同义反复，不是证据。** `vl_direct.py:336-342` 把 `expected_rows`/`row_count`/`extracted_rows` 三者全赋成"模型这一页给了几行"，而 legacy 的 `expected_rows` 来自 OCR 的 `<tr>` 计数（对应有多少行的**独立测量**）。VL 路径没有独立来源，台账**在结构上不可能报出丢行**——实测造一个 3 页只出 1 页行的场景，仍报 `dropped_rows: 0, empty_pages: []`。补独立计数之前，任何报告不得声称"未丢行"（见 docs/design/21 §2.1） |
| 「行级来源页可作为溯源事实」 | **模型自报的页码在运行间不稳定。** 同一份 15 页 PDF，一次报 p2–15、一次报 p1–14，整体偏移一位。`source_ref.page` 目前不能当作追溯依据，与「像素级 bbox 追溯」目标一并记为缺口 |

**本轮（第三轮）撤回**——这些都是我自己先下结论、后被实测推翻的：

| 撤回内容 | 真相 |
|---|---|
| 「方向预检必须用主模型」 | 论据（flash 把某份判成 270° 导致 −87 万）**不成立**：目视核对后确认**flash 判的 270° 是对的**，主模型的 90° 才是错的。选型理由另有其事，见 §2 |
| 「180° 与 0° 对识别等价，只判横竖就够」 | 单页倒置确实 23/23 全对、整批统一倒置只差 −239 元，但**同一份内方向混杂**时损失 **−129,532**（单变量对照实测） |
| 「180° 混杂的代价是 −25.6 万」 | **高估。** 那次同时混入了轴向差异。单变量对照下纯 180° 的代价是 **−129,532** |
| 「提示词说明目的比做选择题好」 | 只在**判横竖**这个子问题上成立（flash 8/11→11/11）。推广到判完整方向就不成立——纯二元问「这页倒了吗」三个模型都只有 3/11 |
| 「四选一 · flash 单次 10/11，可以用」 | **用单次结果做选型。** 完整七份上它漏判 6 页 180°，金额差 0.05 元 → **55,952 元** |
| 「用行内算术闭合反验方向」 | **无效。** 转正与倒置抽出来的行闭合率都是 25/25——模型读倒置页一样准，账自然一样平。且耗时 396s→1164s |
| 「合计行自校验会拦住 13 万这种量级并拒绝认证」 | **完全错误，最严重的一条。** `document_total()` 在**评测脚本里**，生产链不调用。生产的 checksum 在 `db.commit()` **之后**执行（[quote_confirmation_service.py:591](apps/api/services/quote_confirmation_service.py:591) → :598），阈值 **5%**，只写 `job.result` 不阻断。宏胜的 129,532 元是 **0.63%**，会被判 `pass` 并正常入库。**当前没有任何生产保护能拦住它** |
| 「四道入库门已接入」 | **只接了两道。** 重复、算术在确认阶段能重算；**列错位**依赖上游打 `column_shift` flag 而生产链**没有 producer**；**截断**需要原始字符串，确认阶段通常已是数字。准确说法是「四个检查器，两道主门已接入」 |
| 「逐字段全对 96%」 | **名不副实。** [model_bakeoff_compare.py:56](scripts/model_bakeoff_compare.py:56) 只检查 `qty/unit_price/total_price` 三个数值字段，**名称、规格、单位、序号一个都没查**——而规格文本正是本轮反复出问题的地方。应叫「**数值三元组全对率**」 |

**第二轮撤回**（仍然有效，不得作为前提）：

| 撤回内容 | 真相 |
|---|---|
| 「上海浦东重复抽取、金额虚增 42%」 | **不是重复。** PDF 第 9–15 页是整份清单的**第二份**（带抬头红章），golden 只誊录了第一份。3.7 系列的 `copy_no` 标注正确（128/128）。是评分器把两套都加进求和 |
| 「亨通钱对、行对、只是规格文本错」 | 逐行合价在 0.02 容差下只有 60% 对得上；总额接近是因为错误形态是 8 字符截断，只丢小数位 |
| 「块的金额是跨文档最稳的键」 | **生产里不成立。** 招标采购清单只有序号/名称/规格/单位/数量，**没有价格**。用金额对块 = 拿答案对答案。改用**数量序列** |
| 「GPT 能直接吃 PDF，可以省掉渲染」 | `responses.create + input_file` 确实能解析 PDF，但**抽错表**：某份只出 11 行（封面表）、另一份 3 行（分类汇总表）。加规则更差，还撞 Cloudflare 524 |
| 「宏胜匹配 109/136」 | 那是**按规格文本**对齐的结果。按行序保序对齐是 **135/136**，27 行"缺失"里 **0 行真丢** |

---

## 1. 当前实测基线

配置：**qwen3.7-plus 抽取 + 同模型四选一方向预检 + 3 轮投票**，整份一次调用。

> ⚠ **本节是第三轮的离线原型数据，保留作为历史。** 那时整条链在脚本里、生产默认
> 仍是旧模型。现在 VL-direct 已是生产唯一路径（见上方"现状速览"），本节数字应
> 理解为「**离线原型的一次运行**」，既不是当时的系统能力，也不是现在的。

下表数据来自 `tmp/vl_37plus_rot`（**产物未绑定代码 SHA / PDF SHA / 提示词 / 投票轮次 /
最终 rotation map / 缓存状态**——目前无法从干净仓库复现，这是待补的验收基础）：

```bash
python scripts/vl_direct_bakeoff.py --auto-rotate --jobs 7 --out tmp/vl_37plus_rot
python scripts/model_bakeoff_compare.py tmp/vl_37plus_rot=3.7-plus
python scripts/block_align_report.py --out tmp/vl_37plus_rot
```

### 1.1 金额与数值三元组（vs provisional golden）

**指标口径**：「全对」= `qty / unit_price / total_price` 三个数值字段都对，
**不含名称、规格、单位、序号**。浦东按单套副本评分。golden 是
`user_multimodal_provisional`，不是审计过的最终真值。

| 文档 | 明细 | 应有 | 匹配 | 三元组全对 | **合价求和差** |
|---|---:|---:|---:|---:|---:|
| 上海浦东 | 136 | 136 | 132 | 130 | **±0.00** |
| 亨通 | 136 | 136 | 135 | 135 | **±0.00** |
| 宏胜 | 138 | 136 | 114 | 112 | **+0.04** |
| 远东 | 136 | 136 | 136 | 132 | **±0.00** |
| 凯硕新正 | 89 | 89 | 89 | 89 | **±0.00** |
| 上海绵存 | 89 | 89 | 89 | 89 | **±0.00** |
| 泰科龙 | 90 | 89 | 89 | 89 | **−0.01** |

**数值三元组全对 776/811 = 96%；金额差绝对值合计 0.05 元。**

⚠ **这是一次跑的结果，不是稳定值。** 同配置另一次跑，亨通从 ±0.00 变成 −39,466、
匹配从 135 掉到 113（方向判定完全相同，纯抽取波动）。**不要拿这张表当承诺。**

### 1.2 跨供应商块级对齐（锚点侧不含价格）

| 文档 | 对齐率 | pending |
|---|---:|---:|
| 亨通 / 凯硕 / 绵存 / 泰科龙 | **100%** | 0~1 |
| 远东 | 97% | 4 |
| 宏胜 | 96% | 10 |
| **上海浦东** | **68%** | 80 |

浦东退化原因**未查明**（金额 ±0.00、匹配 132/136，块级只有 68%），怀疑与两份副本
和类目分块的交互有关。

### 1.3 模型横向（均带方向转正，七份）

| 模型 | 数值三元组全对 | 金额差绝对值合计 | 总耗时(七份之和) |
|---|---:|---:|---:|
| qwen3-vl-plus | 48% | — | 1,458s |
| qwen3.7-flash | 92% | 24,622 元 | 1,940s |
| **qwen3.7-plus** | **96%** | **0.05 元** | 3,832s |
| qwen3.8-max | 96% | 1,300 元 | 5,949s |

3.8-max 更慢更贵却不更准。耗时是**七份之和**，七份并发的墙钟 ≈ 最慢那份（plus 为 769s）。

---

## 2. 方向预检：完整实验记录

**这是本轮花费最多、反复最多的地方，全部记下来避免重走。**

### 2.1 为什么方向重要

| 文档 | 不转正 | 转正后 |
|---|---:|---:|
| 宏胜 | +1,765,996 | **+0.04** |
| 泰科龙 | 完全不可用（匹配 2） | **89/89 匹配，−0.01** |
| 亨通 | −51,651 | **±0.00** |
| 七份汇总（flash） | 逐字段 67%、金额差 290 万 | 逐字段 92%、金额差 2.5 万 |

意外收获：某份 53 页文档转正后**内容审核误杀消失**，整份一次通过，不再需要分段。

### 2.2 判定方式的九次尝试

| # | 方案 | 结果 | 结论 |
|---|---|---|---|
| 1 | 问「需要顺时针转多少度」 | 宏胜实际 180°，答 90° | ❌ 心算角度不可靠 |
| 2 | **四选一**（4 个旋转版本并排挑正立的） | 主模型宏胜判对 | ✅ 采用 |
| 3 | 3 轮投票 + 无过半共识就不转 | 压住部分波动 | ✅ 保留 |
| 4 | 方向结论按 PDF 哈希缓存 | 缓存过休眠期间的坏结论 → −71 万/−20 万 | ⚠ 改为**全页共识才写盘** |
| 5 | 换 flash 省时间 | 完整七份 **55,952 元**（vs 主模型 0.05 元） | ❌ 撤回 |
| 6 | 目的式英文提示词 + 1 图/页（只判横竖） | flash 单次 **11/11**；完整七份 **309,526 元** | ❌ 丢了 180° 维 |
| 7 | 两问（横竖 + 转正后判倒置） | 宏胜 −255,714 | ❌ 第二问对"本来横排的倒置页"失效 |
| 8 | **稳定性对照**（4 方案 × 2 文档 × 3 次） | **全部方案都不稳定** | 见 §2.3 |
| 9 | 行内算术闭合反验 | 180° 与 0° 都是 25/25，**平局** | ❌ 判据无效，耗时 396→1164s |

### 2.3 稳定性对照（决定性的一次实验）

真值只用**目视逐页核对过**的页。宏胜 p2–11、浦东 p3–15。

| 方案 | 文档 | 三次准确率 | 轴向错 | 三次一致 | 平均耗时 |
|---|---|---|---|:-:|---:|
| 四选一 · plus | 宏胜 | **3 / 10 / 10** | 1/0/0 | 否 | 61s |
| 四选一 · plus | 浦东 | 10/10/10（共13） | 0 | **是** | 38s |
| 四选一 · flash | 宏胜 | **8 / 3 / 10** | 2/0/0 | 否 | 24s |
| 四选一 · flash | 浦东 | 12/10/12（共13） | 0 | 否 | 31s |
| 两问+对比图 · plus | 宏胜 | 10 / 9 / 10 | 0/1/0 | 否 | 74s |
| 两问+对比图 · plus | 浦东 | **5/5/5**（共13） | 0 | 是（稳定地错） | 45s |
| 两问+对比图 · flash | 宏胜 | **3 / 10 / 10** | 0 | 否 | 47s |
| 两问+对比图 · flash | 浦东 | 8 / 10 / 10（共13） | 0 | 否 | 202s |

**结论**：
1. **四个方案没有一个稳定**，包括当前采用的（3/10 → 10/10）。`temperature=0` 挡不住。
2. **轴向（横竖）几乎不出错**：24 次里只有 3 次轴向错，其余全部错在 180° 上。
3. 3 轮投票压不住 3/10 这种崩塌——只降低概率，不消除。
4. **我之前所有基于单份单次的方向选型结论都不可信**，包括让我否掉 flash 的那张表。

### 2.4 各维度的代价（单变量对照）

宏胜，两组唯一差别是 p3–9 的 180°（轴向两组都正确）：

| 组 | 明细 | 匹配 | 四项全对 | 合价求和差 |
|---|---:|---:|---:|---:|
| 方向全对 | 138 | 114 | 112 | **+0.04** |
| 仅轴向对（180° 全错） | 137 | 98 | 93 | **−129,532** |

| 维度 | 判定可靠性 | 判错代价 |
|---|---|---|
| **轴向（横竖）** | 24 次只错 3 次 | 整页读不出，代价极大 |
| **180°** | 极不稳（3/10 vs 10/10） | **−129,532**（0.63%） |

补充事实：**单页倒置对模型无害**——3.7-plus 读 180° 倒置页 23/23 逐字段全对、金额 ±0.00，
与正立完全一致。**危害只在整份混排时出现**（行的归属和数值配对乱掉），这也是算术闭合
反验失效的原因。

### 2.5 已否决、别再试

- **投影法/墨迹能量免费定轴向**：表格框线压过文字行信号，90° 页被判成横排。
- **`/Rotate` 元数据**：实测 7 页报价表全部 180° 倒置而 `/Rotate` 全是 0；页面是纯扫描图，
  `get_text()` 为空。
- **qwen3-vl-plus 做方向预检**：虽快 15 倍（2.6s），但四种提示词 + 开 think 都稳定漏判
  真正侧躺的页（p10/p11 五轮八种配置全漏），系统性偏差，投票压不住。
- **算术闭合反验 180°**：平局，无效。

### 2.6 当前状态与建议

**当前无兜底，这是最大的敞口。** 方向预检保留四选一 + 3 轮投票（轴向可靠、180° 尽力），
但**生产链没有任何东西能拦住 180° 判错造成的 0.63% 误差**：合计行自校验只存在于评测脚本，
生产 checksum 在 commit 之后、阈值 5%、只记录不阻断。**必须先把这道门补上**（见 §5）。

**唯一还没试过的思路**：同一页两个角度各抽一次，比较两次输出与其他供应商同一行的一致性。
成本翻倍且依赖跨文档信息，未评估。

---

## 3. 其他已验证的事实

### 自校验：不需要 golden 就能说出自己错了多少（**仅离线，未接生产**）

明细求和 vs **该产物自己抽到的合计行**（`cable_diff_report.document_total`）：
**8/11 的自校验差与真实差完全相等**，其余会主动报「无法闭环」。
⚠ 这是**评测脚本里的函数**，生产链不调用；且它默认容差 1%、只诊断不阻断。
清单常分章节各出一个合计，故候选包含「单一合计」与「章节合计之和」两种。

### 提示词

约束分两类：**告诉模型「怎么看」有害**（"按原始左右顺序输出列"让侧向页整张表转置）；
**告诉模型「输出什么」有用**（业务语义，模型无从推断）。

现行三条业务规则（`PROMPT_DOC`）：
1. 保留小计/合计/总计行并标 `row_type` —— 换来免 golden 的闭环自检；
2. 不许补算合价，读不到就留空；
3. 同一清单重复出现时照实全部输出，标 `copy_no`。

规则 ③ 在 3.7 系列上工作正常（浦东 128/128 两份副本）；旧模型 3-vl-plus 会把它当页
计数（1..9），那时不能用它筛行。

**注意**：这条原则**不能推广到方向判定**——§2 里"说明目的"的写法只在判横竖时更好。

### LLM 文本复核会改坏真值

实测：把抽好的 CSV 加上「明细合计 X、声明总价 Y、差 Z」交给模型修正，它**没有编造行**
（正确），但把**官方声明总价改成了抽错的那个数**，并给出"应以 X 为准"的结论。后果是
唯一的真值被抹掉、闭环从此永远通过、缺口永久隐藏。

**文本层复核解决不了税基取错、字段留空**——从文本看它们完全自洽，矛盾只存在于原始页面。
正确做法：闭环定位 → 圈可疑行 → **回像素重读** → 重新闭环。
**LLM 的职责是「再看一眼原图并转录」，不是「把账做平」。**

### GPT（gpt-5.6-sol，经第三方中转）

| 文档 | gpt-5.6-sol 图像 | qwen3.7-flash 图像 |
|---|---:|---:|
| 上海浦东 | **136/136 · ±0.00** | 131/136 · −22,534 |
| 远东 | 136/136 · +15,000 | **136/136 · ±0.00** |
| 亨通 | 132/136 · **−145,043** | **135/136 · +532** |
| 宏胜 | 44/136 · **+116 万** | **109/136 · +0.04** |

GPT 赢 1 份、qwen 赢 3 份，**不是单向压制**。PDF 直传已否决（见 §0）。
注意：这些是 API 单次调用的结论，ChatGPT 应用可能不同（未验证）。

### VL API 约束

- DashScope 不能直接发 PDF（`image=PDF` → 400「图像格式非法」）。
- 必须流式，否则整份撞 SDK 300s 读超时。
- 关思考：同页开/关都是 14 行，耗时 104s vs 52s。
- `pypdfium2` **不是线程安全的**：七路并发渲染触发原生崩溃（WinError 0xc000001d），
  整个进程死掉。已加全局渲染锁。

---

## 4. 本轮新增的代码

| 位置 | 说明 |
|---|---|
| `apps/api/services/draft_integrity.py` | **入库前四道门**：列错位 / 重复行 / 算术闭合 / 数值截断 |
| `apps/api/services/block_alignment.py` | **两级对齐**：块级对应（数量序列 → LLM → 顺序回退）+ 块内保序对齐 + 冲突行 pending |
| `apps/api/tests/test_draft_integrity.py` | 34 条 |
| `apps/api/tests/test_block_alignment.py` | 16 条 |
| `scripts/vl_direct_bakeoff.py` | 方向预检（四选一/投票/缓存/验证）、并行、OpenAI 兼容通路、`--rotate-map` 单变量对照 |
| `scripts/model_bakeoff_compare.py` | 多模型横向 |
| `scripts/block_align_report.py` | 块级对齐验证（锚点侧不含价格） |

四道门的关键设计（都有实测反例支撑）：

- **列错位**一行即阻断——没有合法形态；
- **重复行**只标注，金额占比 >10% 才阻断——**合法重复真实存在**（同型号阀门同量同价
  出现在给水和排水两个系统），第一版无脑阻断打红了 4 个原本通过的集成测试；
- **算术**按税基成对取值、倍率判为报价口径不判错误、三缺一记 `not_evaluable` 不算通过；
- **截断**逐列自校准，**整数列永不命中**，7 份 golden 上误报 **0**。

块级对齐的关键设计：**块级键用数量序列**（招标清单有数量、没价格）；判不了才交 LLM，
且 LLM **只判块与块的对应关系**，拿不到行；拒答就按文档顺序回退并标注。

`compute_quality` 里重复的算术实现已改为调用共享的 `check_row_arithmetic`。
**残留**：`routes/analysis.py` 的匹配质量门仍有自己一份（VAT 语义），未合并。

⚠ **`scripts/` 被 `.gitignore` 排除**，本轮的 4 个脚本未提交，但 HANDOFF 的复现命令
全依赖它们。需要决定是否 `-f` 强制加入。

---

## 5. 未完成 / 下一步

1. **方向 180° 的稳定性**（§2.6）——最大的未解风险。
2. **查浦东块级对齐 68% 的原因**（本地数据，零成本）。
3. **接生产路径**：`block_alignment` 尚未接入 `anchor_match`。现有顺序直连要求
   `dn_cov >= 0.90`，**DN 是阀门口径，四份电缆一份都进不去**——这道门必须泛化。
4. **抽取端补页码 + 页内行序**。VL 产物既没有 `document_row_index` 也没有页码归属，
   `_doc_order` 的三态逻辑拿不到输入。
5. **用真实招标清单的 TenderAnchor 做端到端验收**。当前块级对齐用 golden 的
   名称/规格/单位/数量模拟锚点侧，来源仍是同一批文档。
6. **定向重读闭环**（设计已定，未实现）。
7. **规格文本归一**：字母混淆（Y/V、O/0、1/I）按国标型号系列纠正；类目前缀剥离到名称列。
   规格文本应降级为**校验**而非对齐主键。

**已知红灯**：全量 `apps/api/tests` **35 failed / 613 passed**。26 条是派生金额门的爆炸
半径（既有），9 条是 `test_cable_golden`——**那四条仍在跑多阶段管线快照重放**，量的是一条
已不打算走的路。要让验收有意义，必须把 `_recognized()` 指向 VL 产物。

---

## 6. 方法论教训（本轮代价最大的部分）

1. **不要用单次结果做决策。** 本轮至少三次因此走错：选 flash 做方向（单次 10/11 → 七份
   55,952 元）、砍掉 180° 维（单次实验 → 七份 309,526 元）、选"两问+对比图"（单次 11/11）。
   稳定性对照做出来才发现**所有方案都在 3/10 到 11/11 之间跳**。
2. **换尺子不算进步，用错尺子也会低估。** 「宏胜 109/136」是规格文本对齐的结果，
   按行序保序对齐是 135/136。评分口径必须与业务口径一致。
3. **先做单变量对照，再下因果结论。** "180° 代价 −25.6 万"是把轴向差异算进去了，
   单变量对照下是 −129,532。
4. **验证判据必须能区分你要区分的东西。** 算术闭合验证不了 180°，因为模型读倒置页
   一样准——账一样平。选判据前先问"这个判据在错误情形下会不会也通过"。
5. **不确定性必须留在数据里。** 方向缓存曾把"检测全崩"存成"全都不用转"，因为返回值
   把"判定为 0°"和"没有共识"混为一谈。
