# HANDOFF — 识别链路现状与交接

> 更新：2026-08-09（第二轮）· 分支 `codex/agent-rebuild`
> 一句话：**方向转正 + qwen3.7-plus 之后，七份文档的金额差合计 0.05 元**；跨供应商块级
> 对齐五份 100%、两份 96~97%、一份 68%。金额这条线基本走通，**对齐这条线还没打穿**。

---

## 0. 先读这一节：本轮撤回的结论

以下说法在本轮被证伪，**不得作为前提继续使用**：

| 撤回内容 | 真相 |
|---|---|
| 「上海浦东重复抽取、金额虚增 42%」 | **不是重复。** 该 PDF 第 9–15 页是整份清单的**第二份**（带抬头和红章），golden 只誊录了第一份。3.7 系列的 `copy_no` 标注正确（128/128）。是评分器把两套都加进求和 |
| 「亨通钱对、行对、只是规格文本错」 | 逐行合价在 0.02 容差下只有 60% 对得上；总额接近是因为错误形态是 8 字符截断，只丢小数位 |
| 「块的金额是跨文档最稳的键」 | **在生产里不成立。** 招标采购清单只有序号/名称/规格/单位/数量，**没有价格**。用金额对块 = 拿答案对答案。改用**数量序列** |
| 「GPT 能直接吃 PDF，可以省掉渲染」 | API 层 `responses.create + input_file` 确实能解析 PDF，但**抽错表**：某份只出 11 行（封面表）、另一份只出 3 行（分类汇总表）。加规则更差，还撞 Cloudflare 524。**已否决**（ChatGPT 应用可能不同，未验证） |
| 「宏胜匹配 109/136」 | 那是**按规格文本**对齐的结果。按行序保序对齐是 **135/136**，27 行"缺失"里 **0 行真丢**，全是规格文本差异（`RTXMY`→`RTXMV`、类目前缀归属） |

**教训**：换尺子不算进步，但**用错尺子也会低估**。评分口径必须与业务口径一致——比价要的是
跨文档对齐，不是单份 vs golden 的文本匹配。

---

## 1. 当前实测基线

配置：**qwen3.7-plus 抽取 + 同模型方向预检 + 3 轮投票**，整份一次调用。
方向预检**不能换快模型**，原因见 §2。

```bash
python scripts/vl_direct_bakeoff.py --auto-rotate --jobs 7 --out tmp/vl_plus_rot
python scripts/model_bakeoff_compare.py tmp/vl_plus_rot=3.7-plus
python scripts/block_align_report.py --out tmp/vl_plus_rot
```

### 1.1 金额与逐字段（vs golden）

| 文档 | 明细 | 应有 | 匹配 | 逐字段全对 | **合价求和差** |
|---|---:|---:|---:|---:|---:|
| 上海浦东 | 136 | 136 | 132 | 130 | **±0.00** |
| 亨通 | 136 | 136 | 135 | 135 | **±0.00** |
| 宏胜 | 138 | 136 | 114 | 112 | **+0.04** |
| 远东 | 136 | 136 | 136 | 132 | **±0.00** |
| 凯硕新正 | 89 | 89 | 89 | 89 | **±0.00** |
| 上海绵存 | 89 | 89 | 89 | 89 | **±0.00** |
| 泰科龙 | 90 | 89 | 89 | 89 | **−0.01** |

**逐字段全对 776/811 = 96%；金额差绝对值合计 0.05 元。**

### 1.2 跨供应商块级对齐（锚点侧不含价格）

| 文档 | 对齐率 | pending |
|---|---:|---:|
| 亨通 / 凯硕 / 绵存 / 泰科龙 | **100%** | 0~1 |
| 远东 | 97% | 4 |
| 宏胜 | 96% | 10 |
| **上海浦东** | **68%** | 80 |

浦东退化原因**未查明**（金额 ±0.00、匹配 132/136，但块级只有 68%），怀疑与两份副本
和类目分块的交互有关。**这是下一个要查的**。

### 1.3 模型横向（均带方向转正）

| 模型 | 逐字段全对 | 金额差绝对值合计 | 总耗时(七份之和) |
|---|---:|---:|---:|
| qwen3.7-flash | 92% | 24,622 元 | 1,940s |
| **qwen3.7-plus** | **96%** | **0.05 元** | 3,832s |
| qwen3.8-max | 96% | 1,300 元 | 5,949s |

3.8-max 更慢更贵却不更准，**没有理由选它**。耗时是**七份之和**，七份并发的墙钟 ≈ 最慢
那份（3.7-plus 为 769s）。

---

## 2. 已验证的事实（别再花钱重新发现）

### 方向是最大的单一根因

同一份文档转正前后：明细求和差 **+1,765,996 → +0.04**；另一份从完全不可用（匹配 2）
到 **89/89 全匹配**。七份汇总：逐字段全对 67% → 92%（flash），金额差 290 万 → 2.5 万。

- **判定方式必须是「四选一挑正立的那张」**，不能问「需要转多少度」。后者实测答错：某份
  实际倒置 180°，模型答 90°。判别哪张图正立是纯感知题，比心算角度稳。
- **单轮不稳，必须投票**。同一份两次预检结论不同（一次 3–9 页全 180°，一次只有 3 和 9）；
  另一份被误转后逐字段全对从 83 掉到 51。**3 轮投票 + 无过半共识就不转**。
- **/Rotate 元数据不可用**：实测 7 页报价表全部 180° 倒置而 `/Rotate` 全是 0；页面是纯
  扫描图，`get_text()` 为空。
- **投影法/墨迹能量免费定轴向**：已否决（表格框线压过文字行信号），别再试。
- **方向预检必须用主模型**。试过换 flash 省时间（它的载荷是大头：53 页 × 4 旋转 ×
  3 轮 = 636 张缩略图），实测不成立：某份的 270° 是 flash 无中生有（主模型判 90° 正确），
  该份金额差从 ±0.00 变成 **−874,443**；另一份 7 页 180° 倒置它这次没判出来、上次判出来了。
  **三轮投票会一致收敛到错误答案**——投票压得住随机噪声，压不住系统性偏差。
  省下的约 100 秒不值这个代价。
- 方向结论按 **PDF 内容哈希**缓存（`tmp/orientation_cache/`），但**只在每一页都达成共识时
  才写盘**。曾经缓存过一次电脑休眠期间产生的坏结论（检测全崩 → 被当成"全都不用转"），
  之后两份文档的金额差从 ±0.00 变成 −71 万和 −20 万。
  注意这道门只挡得住"检测崩了"，**挡不住"检测一致地错了"**——换模型那次就是共识 100%
  却全错。缓存前最好人工抽查一页。
- 意外收获：某份 53 页文档转正后**内容审核误杀消失**，整份一次通过，不再需要分段。

### 提示词

约束分两类，效果相反：**告诉模型「怎么看」有害**（「按原始左右顺序输出列」让侧向页整张
表转置）；**告诉模型「输出什么」有用**（业务语义，模型无从推断）。

现行三条业务规则（`PROMPT_DOC`）：
1. 保留小计/合计/总计行并标 `row_type` —— 换来免 golden 的闭环自检；
2. 不许补算合价，读不到就留空；
3. 同一清单重复出现时照实全部输出，标 `copy_no`。

**规则 ③ 在 3.7 系列上工作正常**（浦东 128/128 两份副本），旧模型 3-vl-plus 会把它当页
计数（1..9），那时不能用它筛行。

### 自校验：不需要 golden 就能说出自己错了多少

明细求和 vs **该产物自己抽到的合计行**（`cable_diff_report.document_total`）：实测
**8/11 的自校验差与真实差完全相等**，其余会主动报「无法闭环」拒绝认证。
清单常分章节各出一个合计，故候选包含「单一合计」与「章节合计之和」两种。

### LLM 文本复核会改坏真值

实测：把抽好的 CSV 加上「明细合计 X、声明总价 Y、差 Z」交给模型修正，它**没有编造行**
（正确），但把**官方声明总价改成了抽错的那个数**，并给出「应以 X 为准」的结论。后果是
唯一的真值被抹掉、闭环从此永远通过、缺口永久隐藏。

**文本层复核解决不了税基取错、字段留空这类问题**——从文本看它们完全自洽，矛盾只存在于
原始页面。正确做法是：闭环定位 → 圈可疑行 → **回像素重读** → 重新闭环。
**LLM 的职责是「再看一眼原图并转录」，不是「把账做平」。**

### VL API 约束

- 不能直接发 PDF（DashScope 侧 `image=PDF` 返回「图像格式非法」）。
- 必须流式，否则整份撞 SDK 300s 读超时。
- 关思考：同页开/关都是 14 行，耗时 104s vs 52s。
- `pypdfium2` **不是线程安全的**：七路并发渲染触发原生崩溃（WinError 0xc000001d），
  整个进程死掉。已加全局渲染锁——渲染只占总耗时几个百分点，串行化代价可忽略。

---

## 3. 本轮新增的代码

| 位置 | 说明 |
|---|---|
| `apps/api/services/draft_integrity.py` | **入库前四道门**：列错位 / 重复行 / 算术闭合 / 数值截断。全部「只标注和阻断，不删行不改值」 |
| `apps/api/services/block_alignment.py` | **两级对齐**：块级对应（数量序列 → LLM → 顺序回退）+ 块内保序对齐 + 冲突行 pending |
| `apps/api/tests/test_draft_integrity.py` | 34 条，含泛化陷阱：整数列不得命中截断、稀疏截断仍须发现、税基不得跨组、倍率不得修正 |
| `apps/api/tests/test_block_alignment.py` | 16 条，含：段落顺序相反、一章被切多块、价格不得影响对齐、resolver 拒答/抛异常都不丢行 |
| `scripts/vl_direct_bakeoff.py` | 主模型 3.7-plus、方向预检（同模型）+ 全共识才缓存、并行、OpenAI 兼容通路、PDF 直传（已否决保留） |
| `scripts/model_bakeoff_compare.py` | 多模型横向：行数/逐字段/保序率/耗时 |
| `scripts/block_align_report.py` | 块级对齐验证（锚点侧不含价格） |

四道门的关键设计（都有实测反例支撑）：

- **列错位**一行即阻断——没有合法形态；
- **重复行**只标注，金额占比 >10% 才阻断——**合法重复真实存在**（同型号阀门同量同价出现
  在给水和排水两个系统），第一版无脑阻断打红了 4 个原本通过的集成测试；
- **算术**按税基成对取值、倍率判为报价口径不判错误、三缺一记 `not_evaluable` 不算通过；
- **截断**逐列自校准（宽度上限来自该列自身、小数位基线取该列众数），**整数列永不命中**。
  在 7 份 golden 上误报 **0**。

`compute_quality` 里那份重复的算术实现已改为调用共享的 `check_row_arithmetic`
（全仓单行算术只此一份）。**残留**：`routes/analysis.py` 的匹配质量门仍有自己一份
（带 VAT 语义、作用在已入库 BQL 上），未合并——是已知重复，不是遗漏。

---

## 4. 未完成 / 下一步

**按优先级：**

1. **查浦东块级对齐 68% 的原因**（本地数据，零成本）。
2. **接生产路径**：`block_alignment` 尚未接入 `anchor_match`。现有顺序直连的整表门禁要求
   `dn_cov >= 0.90`，**DN 是阀门口径，四份电缆一份都进不去**——这道门必须泛化。
3. **抽取端补页码 + 页内行序**。现在 VL 产物既没有 `document_row_index` 也没有页码归属，
   顺序直连的三态逻辑（`_doc_order`）拿不到输入。这是自动编号对齐的前提。
4. **用真实招标清单的 TenderAnchor 做端到端验收**。当前块级对齐用 golden 的
   名称/规格/单位/数量模拟锚点侧，字段口径一致但来源仍是同一批文档。
5. **定向重读闭环**（设计已定，未实现）：闭环差 ≠ 0 → 圈出不可评估行 → 只重送这些行所在
   页 → 重新闭环。
6. **规格文本归一**：字母混淆（Y/V、O/0、1/I）按国标型号系列纠正；类目前缀统一剥离到名称
   列。规格文本应降级为**校验**而非对齐主键。

**已知红灯**：全量 `apps/api/tests` **35 failed / 613 passed**。26 条是派生金额门的爆炸
半径（既有），9 条是 `test_cable_golden` 的识别验收——**那四条仍在跑多阶段管线快照重放**，
量的是一条已不打算走的路。要让验收有意义，必须把 `_recognized()` 指向 VL 产物。
在那之前，「核心业务验收 0/4」既不能算数也不能撤销。

---

## 5. 运行间波动

同一配置两次跑，宏胜匹配数 101 vs 109。方向检测已用投票压住，**抽取本身仍有随机性**。
要稳定验收需多次取一致结果，或把波动记为已知风险。**不要用单次结果宣布达标。**
